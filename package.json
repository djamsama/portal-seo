{
  "name": "portal-seo",
  "version": "1.0.0",
  "description": "test omar test moez",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "build:static": "node scripts/build-static-pages.js",
    "serve:static": "node scripts/serve-static-pages.js",
    "replace:statics": "node scripts/replace-static-urls.js",
    "extract:page-zips": "node scripts/extract-page-zips.js",
    "cleanup:pages": "node scripts/cleanup-pages.js",
    "process:pages": "npm run extract:page-zips && npm run extract:page-zips && npm run replace:statics && npm run build:static && npm run cleanup:pages",
    "deploy:scp": "tar --exclude=node_modules --exclude=.git  --exclude=static-pages -czf - . | ssh djamel.samai@198.244.201.115 \"mkdir -p /var/sites/portal-seo && tar -xzf - -C /var/sites/portal-seo\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "adm-zip": "^0.5.12",
    "express": "^5.2.1"
  }
}